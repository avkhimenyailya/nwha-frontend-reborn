export interface Thing {
    id?: number;
    fileUrl?: string;
    description?: string;
    archived?: boolean;
    removed?: boolean;
    profileTaskId?: number;
    createdAt?: string;
    updatedAt?: string;
}